---
title: Build pipeline
description: See how svebcomponents turns package exports into browser bundles, types, a manifest, and server renderers.
---

`@svebcomponents/build` configures Svelte and tsdown for each package entry.
Svelte compiles component source; svebcomponents supplies attribute metadata,
guards custom-element registration, emits public types, and writes a custom
elements manifest.

## Entry discovery

The CLI reads browser paths from `package.json` exports:

```json
{
  "exports": {
    ".": {
      "types": "./dist/client/FavoriteNumber.d.ts",
      "default": "./dist/client/FavoriteNumber.js"
    },
    "./helpers": {
      "types": "./dist/client/helpers.d.ts",
      "default": "./dist/client/helpers.js"
    }
  }
}
```

The CLI replaces `dist/client` with `src` and finds one source with the same
basename:

| Browser export                    | Source candidate                     | Pipeline        |
| --------------------------------- | ------------------------------------ | --------------- |
| `./dist/client/FavoriteNumber.js` | `src/FavoriteNumber.svelte`          | Custom element  |
| `./dist/client/helpers.js`        | `src/helpers.ts` or `src/helpers.js` | Standard module |

The CLI stops when more than one candidate matches. Export patterns such as
`"./*"` do not provide concrete entries, so those layouts need
[Configuration](/guides/manual-configuration/).

## Build targets

The CLI uses each export condition to select a target:

| Package export            | Output               | Svelte runtime                     |
| ------------------------- | -------------------- | ---------------------------------- |
| Browser `default`         | `dist/client`        | Bundled                            |
| Browser `svelte`          | `dist/client-svelte` | Host resolves the Svelte runtime   |
| Matching `/ssr` `default` | `dist/server`        | Bundled in generated server output |
| Matching `/ssr` `svelte`  | `dist/server-svelte` | Host resolves the Svelte runtime   |

svebcomponents creates the standalone build for a browser export. It adds a
runtime-sharing build for a `svelte` condition. A matching `/ssr` export makes
svebcomponents compile the component for the server and generate an
`ElementRenderer` entry.

Read [Configure package outputs](/publishing/) for the export shapes.

## Component compilation

The browser pipeline performs these steps for each `.svelte` entry:

1. `@svebcomponents/auto-options` derives `customElement.props` from
   `$props()`.
2. Svelte compiles the component with `compilerOptions.customElement: true`.
3. svebcomponents guards Svelte's `customElements.define()` call so repeated
   imports do not throw.
4. tsdown bundles JavaScript and writes declarations.

The standalone browser build bundles bare package imports. The Svelte-aware
build keeps `svelte` and `svelte/*` for the host resolver. Server builds keep
normal Node dependency resolution. Read the
[`@svebcomponents/build` reference](/packages/build/) for dependency controls.

## Element types & manifest

svebcomponents analyzes each direct `.svelte` entry with a literal tag. It
writes:

- `custom-elements.json` for editors and custom-element tools
- a component declaration with the DOM element, props, attributes, and events
- a separate `svelte/elements` augmentation

The component declaration registers its tag in `HTMLElementTagNameMap`:

```ts
import "my-components";

const element = document.querySelector("favorite-number");
element?.increments; // number | undefined
```

The analyzer cannot name a computed tag. It does not analyze a component that a
`.ts` entry re-exports, so export the `.svelte` file as the public component
entry. Read [Authoring components](/authoring/) for the source patterns it
supports.

## Server preparation

For an SSR entry, svebcomponents checks for an adjacent `.ssr.ts` or `.ssr.js`
module. The server renderer calls that preparation hook before rendering and
serializes properties the hook sets for hydration.

Read [Async components and server data](/server-rendering/async/) for the hook
signature and host support.

## Configuration

Create `svebcomponents.config.ts` when export inference cannot represent your
source paths or output directories. [Configuration](/guides/manual-configuration/)
lists the options.
